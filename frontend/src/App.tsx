function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Regatten</p>
        <h1>Frontend is ready</h1>
        <p className="lead">React, Vite, TypeScript, ESLint and Prettier are configured.</p>
      </section>
    </main>
  );
}

export default App;
