services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"

  backend:
    build:
      context: ./backend
    environment:
      DATABASE_URL: ${DATABASE_URL}
      TRACKED_SAILOR_IDS: ${TRACKED_SAILOR_IDS}
    ports:
      - "3000:3000"
